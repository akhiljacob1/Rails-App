/ Books#index
.cancel
  = link_to "Return to book reviews", book_reviews_path, class: "nav-link"
%h3 Search results
.review-container
  .row.row-cols-1.row-cols-md-6.g-4
    - @books.each do |book|
      .col
        %a.card-link{:href => "#{new_book_review_path(book_id: book.isbn)}"}
          .card.h-100
            .image-container
              %img.card-img-top{:alt => "...", :src => "#{book.image_link}"}/
            .card-body
              %h5.card-title= book.title
              %p.card-text= book.authors
